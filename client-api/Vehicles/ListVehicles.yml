info:
  name: ListVehicles
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/vehicles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
